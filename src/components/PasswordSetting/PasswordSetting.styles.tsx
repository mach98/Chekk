import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        padding: 20,
    },
    iconText: {
        paddingLeft: 10
    },
    iconStyle: {
        color: '#01173b'
    }
})

export default styles;